<div class="hero-unit">
  <h1>Bloccit</h1>
  <p>Bloccit is a resource for sharing links with your friends!</p>

  <% unless user_signed_in? %>
  <p>
      <%= link_to "Sign In", new_user_session_path, class: 'btn btn-primary btn-large' %> or 
      <%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-primary btn-large' %> today!
  </p>
  <% else %>
  <p>
    <%= link_to "View Topics", topics_path, class: 'btn btn-primary btn-large' %>
  </p>
  <% end %>
</div>

<div class="row">
  <div class="span4">
    <h2>Amazing Point 1</h2>
    <p>This is some content about how great point 1 is. You should really use Bloccit because of it! No seriously, it really is amazing and great and you should really check it out.</p>
  </div>
  <div class="span4">
    <h2>Amazing Point 2</h2>
    <p>This is some content about how great point 2 is. You should really use Bloccit because of it! No seriously, it really is amazing and great and you should really check it out.</p>
  </div>
  <div class="span4">
    <h2>Amazing Point 3</h2>
    <p>This is some content about how great point 3 is. You should really use Bloccit because of it! No seriously, it really is amazing and great and you should really check it out.</p>
  </div>
</div>

<div class="span4">
<h1>Latest Posts:</h1>
<% @posts.each do |post| %>
<div class="media">
  <%= render partial: 'votes/voter', locals: { topic: post.topic, post: post } %>
  <div class="media-body">
    <h4 class="media-heading">
      <%= link_to (markdown post.title), [post.topic, post] %>
      <%= image_tag(post.image.thumb.url) if post.image? %>
      <small>
        <%= image_tag(post.user.avatar.tiny.url) if post.user.avatar? %>
        submitted <%= time_ago_in_words(post.created_at) %> ago by 
        <%= post.user.name %> in <%= post.topic.name %><br/>
        <%= post.comments.count %> Comments
      </small>
    </h4>
  </div>
</div>
<% end %>
</div>

<div class="span4">
<h1>Top-Ranked:</h1>
<% @posts_all_time.each do |post| %>
<div class="media">
  <%= render partial: 'votes/voter', locals: { topic: post.topic, post: post } %>
  <div class="media-body">
    <h4 class="media-heading">
      <%= link_to (markdown post.title), [post.topic, post] %>
      <%= image_tag(post.image.thumb.url) if post.image? %>
      <small>
        <%= image_tag(post.user.avatar.tiny.url) if post.user.avatar? %>
        submitted <%= time_ago_in_words(post.created_at) %> ago by 
        <%= post.user.name %> in <%= post.topic.name %><br/>
        <%= post.comments.count %> Comments
      </small>
    </h4>
  </div>
</div>
<% end %>
</div>