<%= image_tag(comment.user.avatar.tiny.url) if comment.user.avatar? %>
    <p><%= comment.body %></p><br>
    <p>submitted <%= time_ago_in_words(comment.created_at) %> ago by <%= comment.user.name %></p><br>